# Database Connection URL (PostgreSQL)
DATABASE_URL="postgresql://postgres:mysecretpassword@localhost:5432/zapier?schema=public"

# JWT Secret Key for authentication
JWT_PASSWORD="mysecretjwtkey123"

# Kafka Brokers
KAFKA_BROKERS="localhost:9092"

# Ports
PRIMARY_BACKEND_PORT=3000
HOOKS_PORT=3002
FRONTEND_PORT=3001

# SMTP Credentials (Optional for Email Action - runs in simulation mode if empty)
SMTP_ENDPOINT="smtp.gmail.com"
SMTP_USERNAME=""
SMTP_PASSWORD=""

# AI Agent API Keys (Optional - runs in simulation mode with curated weekly digest if empty)
GEMINI_API_KEY=""
ANTHROPIC_API_KEY=""
OPENAI_API_KEY=""

# Google Calendar OAuth (Optional - runs in simulation mode if empty)
GOOGLE_CALENDAR_ACCESS_TOKEN=""

